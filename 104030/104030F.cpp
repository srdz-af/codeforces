#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    ios::sync_with_stdio(false);
	cin.tie(0),cout.tie(0);
    ll n;
    set<string> teams;

    for(int i = 0; i < n; i++){
        vector<string> tmp;
        for(int i = 0; i < n; i++){
            ll s;
            cin >> s;
            if(s[0] == '*') continue;

        }
    }   
    return 0;
}